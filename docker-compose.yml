services:
  neo4j:
    image: neo4j:5-community
    container_name: belvedere-neo4j
    ports:
      - "7474:7474" # browser UI
      - "7687:7687" # bolt
    environment:
      - NEO4J_AUTH=neo4j/belvedere-dev
    volumes:
      - belvedere-data:/data

volumes:
  belvedere-data:
